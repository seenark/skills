# Rename the canonical skill

The fork will use `codebase-full-course-interactive` as its canonical skill identity instead of `codebase-to-course`. The new identity distinguishes the reusable, research-first interactive developer-course workflow from the original single-page HTML output while the implementation remains a fork during migration.
