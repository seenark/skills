# Keep the skill repository as a simple directory structure

The MVP skill repository will not use Turborepo. The skill, internal phase playbooks, deterministic scripts, contracts, references, and React course template will remain in a simple directory structure until independent package boundaries and build requirements are proven. Generated Course Repositories remain standalone Vite applications without workflow orchestration tooling.
