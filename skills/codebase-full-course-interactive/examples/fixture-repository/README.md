# Course Fixture Pipeline

A tiny source repository used to prove pinned source references, execution traces, branches, and source-linked interactive lessons. `handle` trims and validates a request before dispatching accepted input.
