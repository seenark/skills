# Prerequisites

ก่อนเริ่ม ให้ตรวจว่ามีพื้นฐานต่อไปนี้:

- {prerequisite_1}
- {prerequisite_2}
- เปิด terminal และรัน `node -v` ได้
- เปิด repository ที่ `{pinned_version}` ได้

## Quick environment check

```bash
node -v
pwd
```

ถ้าคำสั่งข้างบนรันไม่ได้ ให้แก้ environment ก่อนเริ่มบทเรียน
